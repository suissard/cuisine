{
  "name": "cuisine-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Root management for Cuisine project",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"docker compose up strapi\" \"npm run dev --prefix frontend\"",
    "start": "docker compose -f docker-compose.yml -f docker-compose.prod.yml up --build -d",
    "clean": "rm -rf node_modules backend/node_modules frontend/node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
